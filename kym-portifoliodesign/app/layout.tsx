import React from "react"
import type { Metadata } from 'next'
import { Geist, Geist_Mono } from 'next/font/google'

import './globals.css'

const _geist = Geist({ subsets: ['latin'] })
const _geistMono = Geist_Mono({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Paul kimoni Electrical Engineer | Power Systems & Control Systems',
  description: 'Professional portfolio showcasing expertise in electrical engineering, power systems, motors, welding, and industrial control systems.',
  generator: 'kym-portifoliodesign',
  themeColor: '#0080ff',
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <body className="font-sans antialiased">{children}</body>
    </html>
  )
}
